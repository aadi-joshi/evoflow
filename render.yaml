services:
  - type: web
    name: evoflow-ai
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: MODEL_NAME_FAST
        value: gpt-4o-mini
      - key: MODEL_NAME_SMART
        value: gpt-4o
